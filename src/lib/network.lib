#www

#Coneccion de internet
    export net="$(ping -c 1 -q www.google.com >&/dev/null; echo $?)"
#laptop
    local=$(hostname -I)
    export ip=${local:0:11}
    export ipp=${ip:0:11}:8080
#phone
    local=$(ifconfig -v wlan0 | grep inet )
    export ipt=${local:13:12}
    export iptp=${ip:13:12}:8080
